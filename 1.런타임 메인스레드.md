1.단어 정의

| 개념       | 정의                                  |
| -------- | ----------------------------------- |
| 콜 스택     | 함수 호출을 순서대로 쌓아두는 자료구조               |
| 단일 스레드   | 동시에 한작업만 처리 가능                      |
| LIFO후입선출 | 나중에 들어간게 먼저 나옴                      |
| 블로킹 연산   | 오래 걸리는 동기 코드가 스택을 계속 차지해서 다른작업을 막는것 |
| 스택 오버플로우 | 콜스택 한계를 넘어서 쌓여서 에러발생                |
|          |                                     |

2.동기 처리 
JS에서 await, setTimeout, Promise.then같은게 안붙은 코드는 전부 동기입니다
동기코드는 콜스택에 올라갔다가, 결과값을 바로 반환하고 즉시 스택에서 빠집니다. 대기열(큐)에 가지않고, 다른작업이 끼어들 틈도없이 바로 처리

| 종류          | 코드                                    | 설명              |
| ----------- | ------------------------------------- | --------------- |
| 일반 변수 할당/연산 | `let x = 1 + 2;`                      | 계산 즉시 끝남        |
| 배열/객체 메서드   | `arr.map(), arr.filter(), arr.sort()` | 결과 즉시 나옴        |
| console.log | `console.log()`                       | 즉시 출력           |
| DOM직접조작     | `element.style.color="red"`           | 즉시반영            |
| JSON파싱/변환   | `JSON.parse(str),JSON.stringify(obj)` | 즉시 처리           |
| 일반함수호출      | `function add(a,b){retrun a+b}`       | 호출즉시결과반환        |
| 반복문         | `for,while,forEach`                   | 끝날때까지 다음코드로안넘어감 |
| 정규식 매칭      | `str.match(/abc/)`                    | 즉시처리            |


아래 url로 들어가시면 더자세한 설명이 있습니다. 
https://inf.run/sWBKU